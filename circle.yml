machine:
  node:
    version: 6.10.3
dependencies:
  override:
    - npm install --verbose
test:
  override:
    - npm test
    - if [[ -e test-results.xml ]]; then cp test-results.xml $CIRCLE_TEST_REPORTS/test-results.xml; fi